BMI calculator trained me for parsing the data like for the input. When you receive an input, the data stores as a string. With parsing, we can change it directly ton manipulate datas easily.

Trained operators too